import { BindBoxDirective } from './bind-box.directive';

describe('BindBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new BindBoxDirective();
    expect(directive).toBeTruthy();
  });
});
