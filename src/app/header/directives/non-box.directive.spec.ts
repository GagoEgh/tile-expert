import { NonBoxDirective } from './non-box.directive';

describe('NonBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new NonBoxDirective();
    expect(directive).toBeTruthy();
  });
});
